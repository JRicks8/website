{
    "compilerOptions": {
        "allowJs": true,
        "checkJs": true,
        "noEmit": true, // Prevent TypeScript from emitting JS files
        "target": "es2024"
    },
    "exclude": ["node_modules"]
}
{
    "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true, // Prevent TypeScript from emitting JS files if you're not using TypeScript compilation
    "target": "es2024"
    },
    "exclude": ["node_modules"] // Exclude node_modules for performance
}